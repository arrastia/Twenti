import { StyleSheet } from 'react-native';

const container = { backgroundColor: '#77b3d8', flex: 1 };
const form = {};
const input = { backgroundColor: 'white', margin: 10, padding: 10 };

export const styles = StyleSheet.create({ container, form, input });
